<div class="participant bg-primary" data-userid="{{usersocketid}}"  data-groupid="{{groupid}}">
    <img src="{{groupimage}}" alt="{{name}}">
    <span class="user-fullname">
        {{name}}
    </span>
    <span class="numgroupusers">
        ({{numgroupusers}})
    </span>
</div>
